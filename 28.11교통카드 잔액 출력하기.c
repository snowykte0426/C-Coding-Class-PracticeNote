/*
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main() {
    int bl = 0;
    scanf("%d", &bl);
    while (bl > 0) {
        bl -= 1200;
        if (bl < 0)
        break;
        printf("%d\n", bl);
    }
}
*/
