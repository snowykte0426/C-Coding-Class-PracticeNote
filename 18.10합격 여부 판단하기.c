/*
#define _CRT_SECURE_NO_WARNINGS 
#include <stdio.h> 
int main()
{
    int a, b, c, d;
    scanf("%d %d %d %d", &a, &b, &c, &d);
    if ((0 <= a) && (a <= 100) && (0 <= b) && (b <= 100) && (0 <= c) && (c <= 100) && (0 <= d) && (d <= 100)) {
        if ((a + b + c + d) / 4 >= 85) {
            printf("합격");
        }
        else {
            printf("불합격");
        }
    }
    else {
        printf("잘못된 점수");
    }
    return 0;
}
*/